# Git 커밋 규약

이 문서는 행운 공방 디펜스 UE5 프로젝트의 커밋 메시지와 변경 단위에 대한 팀 기준이다. 커밋 이력만 읽어도 변경 목적과 영향을 이해하고, 문제가 생겼을 때 필요한 변경을 추적하거나 되돌릴 수 있도록 작성한다.

[Conventional Commits 1.0.0](https://www.conventionalcommits.org/en/v1.0.0/)의 메시지 형식을 사용한다. 제목 길이, 언어, 타입 목록, scope 목록, 검증 기록 방식은 이 프로젝트에서 정한 운영 규칙이다. 이 문서 자체가 Git 훅, CI 검사, 저장소의 병합 설정을 활성화하지는 않는다.

## 1. 빠르게 적용하기

```text
feat(summon): 유닛 1회 소환 기능 추가
fix(network): 중복 요청에 따른 골드 이중 차감 방지
docs(planning): 웨이브 종료 조건 명확화
```

- 한 커밋에는 하나의 목적을 담는다. 관련 코드·테스트·데이터는 함께 포함할 수 있다.
- 제목은 `type(scope): 설명`으로 작성한다. scope는 생략할 수 있다.
- type과 scope는 영문 소문자, 설명과 본문은 한국어를 기본으로 한다.
- 제목은 접두사를 포함해 50자 안팎을 권장하고, 최대 72자로 작성한다.
- 복잡한 변경은 본문에 이유와 영향을 기록한다. 실행한 검증은 커밋 본문 또는 PR에 남긴다.
- 기존 데이터·저장 파일·네트워크 규약 등의 호환성을 깨면 `!`와 `BREAKING CHANGE:`를 함께 쓴다.

## 2. 메시지 형식

```text
<type>[(scope)][!]: <변경 요약>

<변경 이유와 주요 영향 — 필요할 때 작성>
<검증 결과 — 커밋 본문 또는 PR에 작성>

<footer — 관련 이슈, 호환성 변경 등이 있을 때 작성>
```

대괄호는 선택 항목을 나타내며 실제 메시지에 입력하지 않는다. 제목·본문·footer 사이는 빈 줄로 구분한다.

### 제목

- 콜론 뒤에는 공백 한 칸을 둔다.
- 무엇이 어떻게 바뀌었는지 구체적으로 쓴다. `추가`, `수정`, `제거`, `분리`, `방지`처럼 동작을 드러내는 표현으로 끝낸다.
- 마지막에 마침표를 붙이지 않는다. 이모지, 작성자 이름, 날짜는 넣지 않는다.
- `작업`, `수정함`, `완료`, `WIP`, `final`만으로 변경 내용을 대신하지 않는다.
- 클래스명·API·에셋명 등 식별자는 원래 표기를 유지한다.
- 이슈 번호는 기본적으로 footer에 둔다. 병합 도구가 제목에 붙이는 PR 번호는 허용한다.

| 피해야 할 제목 | 권장 제목 |
|---|---|
| `수정` | `fix(board): 보드 밖으로 유닛이 이동하는 오류 수정` |
| `feat: 버그 수정` | `fix(battle): 사망한 적을 공격 대상으로 선택하는 오류 수정` |
| `style(ui): 소환 버튼 디자인 변경` | `feat(ui): 소환 버튼에 재사용 대기시간 표시 추가` |
| `chore: 이것저것 정리` | `refactor(economy): 골드 차감 검증 로직 통합` |

### 본문과 footer

본문은 diff만으로 알기 어려운 **이유, 기존 문제, 변경 후 동작, 영향 범위**를 설명한다. 단순 오탈자 수정은 제목만으로 충분하다. 검증 결과는 실제 수행한 내용만 기록하고, 실행하지 못했다면 이유와 남은 확인 사항을 적는다.

```text
fix(network): 동일 소환 요청의 중복 처리 방지

재전송된 요청을 새 요청으로 처리하면 골드가 중복 차감될 수 있다.
처리한 RequestId의 결과를 재사용해 재화와 소환 결과를 일관되게 유지한다.

검증: 2클라이언트 PIE에서 동일 RequestId를 두 번 전송해 골드 1회 차감 확인
미검증: Android 실기기 재연결 상황

Refs: #42
```

위 검증 문장은 작성 예시이며 현재 구현이나 테스트 결과를 나타내지 않는다. 아래의 기능·에셋·이슈 번호 예시도 같은 원칙을 따른다.

| footer | 용도 |
|---|---|
| `Refs: #42` | 관련 이슈 참조 |
| `Closes: #42` | 이 변경으로 해결을 완료하는 이슈 표시 |
| `BREAKING CHANGE: 내용` | 깨지는 호환성과 필요한 전환 작업 설명 |

이슈가 없다면 번호를 만들거나 footer를 억지로 추가하지 않는다. 이슈 자동 종료 여부는 사용하는 호스팅 서비스와 병합 대상 브랜치 설정을 따른다.

## 3. type 선택 기준

파일 확장자보다 **변경 목적**으로 type을 고른다. 이 표는 팀에서 사용할 타입 목록이며, 모두가 Conventional Commits 표준의 필수 타입인 것은 아니다.

| type | 사용 기준 | 예시 |
|---|---|---|
| `feat` | 새 기능·콘텐츠 추가 또는 의도적인 플레이 동작 변경 | `feat(wave): 보스 웨이브 추가` |
| `fix` | 의도한 동작과 다른 버그 수정 | `fix(save): 결과 보상 중복 수령 방지` |
| `docs` | 문서만 변경 | `docs(planning): 합성 조건 설명 보완` |
| `style` | 동작에 영향 없는 코드 서식 변경 | `style(battle): 전투 코드 들여쓰기 통일` |
| `refactor` | 기능·동작을 유지하는 내부 구조 개선 | `refactor(board): 배치 검증을 별도 함수로 분리` |
| `perf` | 동작을 유지하면서 성능 개선 | `perf(battle): 표적 탐색 시 임시 배열 할당 감소` |
| `test` | 테스트 추가·수정 | `test(economy): 골드 부족 시 소환 거부 검증 추가` |
| `build` | 빌드·패키징·의존성·엔진 또는 플러그인 설정 변경 | `build(android): 패키징 대상 아키텍처 설정 변경` |
| `ci` | CI 파이프라인·자동 검증·배포 워크플로 변경 | `ci: 데이터 검증 단계 추가` |
| `chore` | 위 분류에 속하지 않는 저장소 유지보수 | `chore: Unreal 생성 파일 제외 규칙 정리` |
| `revert` | 기존 커밋의 변경을 되돌림 | `revert(economy): 소환 비용 조정 되돌리기` |

### 혼동하기 쉬운 경우

- **UI·아트 변경**: 화면 기능이나 표현을 의도적으로 바꾸면 `feat`, 잘못된 표시를 고치면 `fix`다. `style`은 코드 서식에만 사용한다.
- **밸런스 변경**: 기획 의도로 비용·확률·피해량을 바꾸면 `feat`, 확정된 값과 다르게 입력된 수치를 바로잡으면 `fix`다. 본문에 변경 전후 값과 이유를 적는다.
- **기능과 테스트를 함께 추가**: 기능 구현에 필요한 테스트는 같은 `feat` 커밋에 넣는다. 별도의 테스트 보강은 `test`다.
- **버그 수정 전 대규모 구조 정리**: 동작을 유지하는 준비 작업은 `refactor`, 실제 동작 수정은 `fix`로 나눈다. 수정에 꼭 필요한 작은 구조 변경은 `fix`에 포함할 수 있다.
- **여러 목적이 섞인 변경**: 독립적으로 검토·되돌릴 수 있으면 커밋을 나눈다. 분리하면 기능이 깨지는 변경은 주된 목적의 type으로 묶는다.

## 4. scope 선택 기준

scope는 변경이 영향을 주는 기능 영역이다. 디렉터리 이름과 반드시 같을 필요는 없다. 다음 목록을 우선 사용하고, 새 영역이 필요하면 팀에서 이름을 맞춘 후 추가한다.

| scope | 영역 |
|---|---|
| `core` | 게임 모드·공통 초기화·핵심 흐름 |
| `battle` | 공격·피해·상태 효과·표적 선택 |
| `board` | 타일·배치·이동 |
| `summon` | 소환·합성·제작 |
| `economy` | 재화·비용·강화·상점 |
| `wave` | 웨이브·적 스폰·보스 진행 |
| `network` | RPC·복제·서버 권한·협동 동기화 |
| `save` | 저장·불러오기·결과 기록 |
| `ui` | HUD·메뉴·버튼·위젯 |
| `input` | 터치·입력 매핑·조작 |
| `data` | 데이터 스키마·검증·여러 영역에 걸친 데이터 처리 |
| `art` / `audio` | 시각 에셋 / 효과음·음악 |
| `android` / `ios` | 플랫폼별 빌드·동작 |
| `planning` | 기획·설계·운영 문서 |

한 제목에는 scope를 하나만 쓴다. 소환 비용처럼 기능 영역이 명확하면 `data`보다 `economy`를 사용한다. 전역 변경이거나 대표 영역을 정하기 어렵다면 scope를 생략하고 본문에 영향을 적는다.

## 5. 커밋 단위와 UE5 파일 관리

### 하나의 목적을 완결된 상태로 묶기

- 각 커밋은 가능한 한 빌드·실행·검증 가능한 상태로 남긴다.
- 기능 구현 코드와 그 기능에 필요한 설정·에셋·테스트·문서는 함께 커밋할 수 있다.
- 기능 수정에 관련 없는 포맷팅, 이름 변경, 다른 버그 수정을 섞지 않는다.
- 파일 개수나 작업 시간으로 나누지 않는다. 리뷰와 되돌리기가 가능한 변경 단위로 나눈다.

예를 들어 소환 기능에 필요한 C++, Blueprint, 입력 설정은 하나의 `feat(summon)`으로 묶을 수 있다. 같은 날 수정한 로비 오탈자는 별도의 `fix(ui)`로 분리한다.

### 코드·설정·에셋

- `Source/`, 팀 공용 `Config/`, `.uproject`, `Content/`의 필요한 원본 변경을 확인한다.
- `Binaries/`, `Intermediate/`, `Saved/`, `DerivedDataCache/` 등 빌드·캐시·개인 작업 생성물은 포함하지 않는다. 실제 제외 규칙은 [`.gitignore`](.gitignore)를 따른다.
- 에디터가 다시 저장한 무관한 에셋이나 개인 환경 설정이 섞이지 않았는지 확인한다.
- `.uasset`, `.umap`은 일반 텍스트처럼 병합하기 어렵다. 같은 에셋을 동시에 수정하기 전에 담당자를 조율하고, 충돌 시 에디터에서 결과와 참조를 검증한다.
- 에셋 이동·이름 변경 시 참조 변경과 필요한 리다이렉터 처리를 함께 확인한다. 경로를 바꾸는 중간 상태만 커밋하지 않는다.
- 큰 바이너리의 LFS 적용 여부는 팀 저장소 정책을 따른다. LFS를 사용한다면 `.gitattributes`에 추적 규칙이 있고 팀원이 해당 에셋을 내려받을 수 있는지 확인한다.
- 키·토큰·서명 인증서·비밀번호·개인 경로가 포함되지 않았는지 확인한다.

Blueprint나 바이너리 에셋 변경은 커밋 본문에 **에셋 경로, 변경 목적, 플레이에 미치는 영향**을 적는다. 시각적 변경은 PR에 전후 화면을 첨부하고, 로직 변경은 재현·검증 절차를 남긴다.

### 기획 데이터

이 저장소의 `data/DT_*.json` 수치 변경은 [기획 패키지의 생성 절차](PLANNING_README.md)에 따라 원본인 `tools/build-design-data.mjs`를 수정하고 생성 결과를 확인한다. 원본과 그 변경에서 나온 결과물은 함께 커밋한다.

```powershell
node tools/build-design-data.mjs
node tools/validate-design-data.mjs
```

생성 명령은 관련 파일을 다시 쓸 수 있으므로 실행 후 diff를 확인한다. 밸런스를 바꾸면 영향을 받는 기획 문서도 맞춘다. `GameRules.json` 등 다른 데이터는 해당 파일의 원본 관리 방식을 확인한 뒤 변경한다.

## 6. 호환성을 깨는 변경

다음처럼 기존 소비자가 그대로 사용할 수 없게 되면 breaking change로 기록한다.

- 이전 저장 파일을 읽지 못하게 되는 저장 형식 변경
- 구버전 클라이언트와 통신할 수 없게 되는 RPC·네트워크 메시지 변경
- 기존 데이터를 읽는 코드나 Blueprint를 함께 수정해야 하는 데이터 필드 제거·타입 변경
- 기존에 지원하던 플랫폼·엔진 버전 지원 종료

일반적인 밸런스 조정이나 내부 함수 정리만으로 `!`를 붙이지 않는다. 변경 규모보다 기존 사용 방식의 호환성이 기준이다.

표준은 `!` 또는 `BREAKING CHANGE:` 중 하나로도 표현할 수 있지만, **이 팀에서는 둘 다 사용**해 제목에서 식별하고 본문에서 전환 방법을 확인한다. type은 변경 목적에 따라 선택한다.

```text
refactor(data)!: 유닛 공격력 필드를 물리·마법 피해로 분리

피해 계산에서 물리와 마법 방어를 각각 적용할 수 있도록 필드를 분리한다.
데이터 생성기, 행 구조체, 데이터 참조 Blueprint를 함께 변경한다.

BREAKING CHANGE: DT_Units의 Attack 필드를 제거한다. 기존 데이터는
  PhysicalAttack과 MagicAttack으로 변환해야 한다. 기존 물리 유닛은
  PhysicalAttack에 이전 Attack 값을 옮기고 MagicAttack을 0으로 설정한다.
Refs: #108
```

전환 작업이 필요한 변경은 PR에도 영향 대상과 적용 순서를 명시한다.

## 7. PR 병합과 이력 정리

### 기본 운영 방식

- 작성자가 만드는 커밋 메시지와 PR 제목에 이 규약을 적용한다.
- 한 PR에는 하나의 리뷰 목적을 담는다. 리뷰 중 추가된 보완 커밋은 허용한다.
- 병합은 **Squash merge를 권장**한다. 이는 운영 권장안이며 현재 저장소의 설정을 뜻하지 않는다.
- Squash merge 시 최종 제목뿐 아니라 본문·이슈 참조·호환성 변경 설명도 검토한다. 중간 커밋의 중요한 설명이 사라지지 않도록 최종 메시지에 옮긴다.
- 개별 커밋을 유지하는 방식으로 병합한다면, 남는 각 커밋의 목적과 메시지를 점검한다.
- Git이나 호스팅 도구가 자동 생성하는 merge commit 제목은 예외로 허용한다.

### 메시지 수정과 되돌리기

아직 push하지 않은 마지막 커밋의 메시지는 다음 명령으로 수정한다. staged 변경이 있으면 함께 포함될 수 있으므로 먼저 확인한다.

```powershell
git diff --cached
git commit --amend
```

공유 브랜치에 올라간 커밋은 메시지 정리만을 위해 이력을 다시 쓰지 않는다. 개인 작업 브랜치도 리뷰 중이라면 협업자와 조율한 후 이력을 정리한다.

이미 병합한 변경을 취소할 때는 새 `revert` 커밋을 만든다. 아래 명령의 `<대상-커밋-SHA>`는 실제 SHA로 바꾼다.

```powershell
git revert --edit <대상-커밋-SHA>
```

편집기에서 제목을 규약에 맞추고 원본 커밋 SHA와 되돌리는 이유를 남긴다. merge commit을 되돌릴 때는 부모 기준을 별도로 판단해야 하므로 위 명령을 그대로 적용하지 않는다.

```text
revert(economy): 소환 비용 조정 되돌리기

초반 웨이브에서 소환 가능 횟수가 과도하게 줄어 이전 비용으로 복구한다.
원본 커밋: <실제 원본 SHA>

Refs: #73
```

## 8. 커밋 전 확인 절차

```powershell
git status --short
git diff
# 필요한 파일을 지정해 stage한다. 텍스트 변경 일부만 선택하려면 git add -p를 사용한다.
git add -- <변경-파일-경로>
git diff --cached --stat
git diff --cached
git diff --cached --check
git commit
```

`<변경-파일-경로>`는 실제 경로로 바꾼다. `git diff --cached --check`는 공백 오류 등을 확인하며, 빌드·테스트나 바이너리 에셋 검증을 대신하지 않는다.

- [ ] 제목의 type·scope가 실제 변경 목적과 일치하는가?
- [ ] 함께 검토하고 되돌릴 수 있는 하나의 목적만 포함했는가?
- [ ] staged diff에 누락된 의존 파일이나 무관한 파일이 없는가?
- [ ] 코드·설정·에셋·데이터·문서의 참조가 서로 맞는가?
- [ ] 필요한 빌드·테스트·플레이 검증을 수행하고 결과 또는 미검증 사유를 기록했는가?
- [ ] 생성물·개인 설정·비밀정보가 포함되지 않았는가?
- [ ] 호환성을 깨는 경우 `!`, `BREAKING CHANGE:`, 전환 방법을 적었는가?

검증 범위는 변경 영향에 맞춘다. 문서는 링크와 내용, 데이터는 생성 결과와 검증 스크립트, C++는 영향받는 타깃 빌드, Blueprint는 컴파일과 관련 플레이 흐름을 확인한다. 멀티플레이 변경은 2클라이언트 동작, 모바일 전용 변경은 해당 플랫폼 검증 결과나 미검증 사유를 남긴다.

## 9. 복사용 템플릿

필요 없는 본문 항목과 footer는 삭제한다. 검증 기록을 PR에 남겼다면 커밋에 같은 내용을 반복할 필요는 없다.

```text
type(scope): 변경 요약

변경 이유:
- 해결하려는 문제 또는 변경이 필요한 배경

주요 변경:
- 핵심 동작과 영향 범위

검증:
- 수행한 검증과 결과 또는 미검증 사유

Refs: #이슈번호
```

이 규약 문서만 커밋한다면 다음 제목을 사용할 수 있다.

```text
docs: Git 커밋 규약 추가
```
